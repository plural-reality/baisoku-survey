# ============================================================
# Supabase Self-Host 環境変数
# generate-keys.sh で自動生成されます。手動設定も可。
# ============================================================

# --- 自動生成される値（generate-keys.sh が埋める） ---
POSTGRES_PASSWORD=your-super-secret-password
JWT_SECRET=your-super-secret-jwt-token-at-least-32-characters
ANON_KEY=your-generated-anon-key
SERVICE_ROLE_KEY=your-generated-service-role-key

# --- ドメイン設定（自分の環境に合わせて変更） ---
# Supabase API の公開 URL（Nginx 経由）
API_EXTERNAL_URL=https://supabase.baisoku-survey.example.com

# Next.js アプリの URL
SITE_URL=https://baisoku-survey.example.com

# 認証コールバックで許可する追加 URL（カンマ区切り）
ADDITIONAL_REDIRECT_URLS=https://baisoku-survey.example.com/auth/confirm

# --- SMTP 設定（マジックリンクメール送信に必要） ---
# Amazon SES, SendGrid, Resend 等の SMTP を使用
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=re_xxxxxxxxxxxx
SMTP_ADMIN_EMAIL=noreply@example.com
SMTP_SENDER_NAME=Sonar

# --- Docker イメージバージョン（必要に応じて更新） ---
# 互換バージョンは https://github.com/supabase/supabase/blob/master/docker/.env を参照
POSTGRES_VERSION=15.8.1.20
GOTRUE_VERSION=v2.164.0
POSTGREST_VERSION=v12.2.3
KONG_VERSION=2.8.1
META_VERSION=v0.84.2
STUDIO_VERSION=20241202-5765e4b

# --- JWT 有効期限（秒） ---
JWT_EXPIRY=3600
